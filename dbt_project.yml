name: dbt_airlines
version: "1.0.0"
config-version: 2

profile: dbt_airlines

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_airlines:
    staging:
      +materialized: view
    dimensions:
      +materialized: table
    facts:
      +materialized: table
